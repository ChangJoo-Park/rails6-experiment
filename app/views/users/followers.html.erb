<div class="mt-3">
  <ul class="list-unstyled">
    <%= render partial: "user_list", collection: @followers, as: :user %>
  </ul>
  <% if @followers.empty? %>
    <h2 class="text-lg">팔로우하는 사용자가 없습니다.</h2>
  <% end %>
</div>