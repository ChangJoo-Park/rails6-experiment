<div class="row">
  <div class="col-3">
    <% if user_signed_in? %>
      <div class="card mb-3">
        <div class="row no-gutters">
          <div class="col-md-4">
          <%= image_tag current_user.user_profile.avatar.variant(resize: "100"), class: "card-img" %>
          </div>
          <div class="col-md-8">
            <div class="card-body">
              <h5 class="card-title"><%= current_user.user_profile.username %></h5>
            </div>
          </div>
        </div>
      </div>
      <!-- Favorites -->
      <div class="mb-3 p-3 border">
        <div class="text-monospace">
          Favorites
        </div>
        <ul class="list-unstyled overflow-auto" style="max-height: 420px;">
          <!-- FIXME: move to controller below -->
          <% if current_user.favorites.includes(:post).order(created_at: :desc).each do |favorite| %>
            <li><%= link_to  favorite.post.title, favorite.post %></li>
          <% end.empty? %>
            좋아하는 글이 없습니다.
          <% end %>
        </ul>
      </div>
    <% end %>

    <div class="mb-3 p-3 border">
      <div class="text-monospace">
        Tags
      </div>

      <ul class="list-unstyled overflow-auto" style="height: 420px;">
      <% @tags.each do |tag| %>
        <li><%= link_to "##{tag.name}", tag_path(tag.name.strip), class: "text-monospace" %></li>
      <% end %>
      </ul>
    </div>

    <div class="mb-3 border">
      <div class="text-center">커뮤니티 스폰서</div>
      <br>
      <div class="text-center">
        <a href="#">첫번째 후원을 해주세요</a>
      </div>
    </div>
    <!-- Key links  -->
    <div class="mb-3 border">
      <div class="text-center">Key Links</div>
      <br>
      <ul class="list-unstyled pl-3">
        <li>About</li>
        <li>스폰서</li>
        <li>스토어</li>
        <li>이용약관</li>
        <li>개인정보</li>
        <li>문의하기</li>
        <li>자주 묻는 질문</li>
        <li>행동강령</li>
      </ul>
    </div>
  </div>
  <div class="col">
    <p id="notice"><%= notice %></p>

    <h1><%= @title %></h1>

    <div>
      <%=
        render partial: "components/post_list_item",
        collection: @posts, as: :post,
        locals: { has_user: true }
      %>
    </div>
    <br>
  </div>
  <%# Large 스크린 이상에서만 노출 %>
  <div class="col-3">
    <div class="mb-3 p-3 border">
      <div class="text-monospace">
        #help
      </div>
      <ul>
        <li>test</li>
      </ul>
      <div class="text-center">
        <%= link_to  "글쓰기", new_post_path(@post, tag: "help"), class: "btn btn-outline-dark" %>
      </div>
    </div>
    <div class="mb-3 p-3 border">
      <div class="text-monospace">
        #discuss
      </div>
      <ul>
        <li>test</li>
      </ul>
      <div class="text-center">
        <%= link_to  "글쓰기", new_post_path(@post, tag: "discuss"), class: "btn btn-outline-dark" %>
      </div>
    </div>
    <div class="mb-3 p-3 border">
      <div class="text-monospace">
        #ama
      </div>
      <ul>
        <li>test</li>
      </ul>
      <div class="text-center">
        <%= link_to  "글쓰기", new_post_path(@post, tag: "ama"), class: "btn btn-outline-dark" %>
      </div>
    </div>
  </div>
</div>
